Пропустил очень важный интерфейс и класс!!!
Поэтому 3.1, т.к. лень все файлы переименовывать, ветку можешь назвать task3_1

интерфейс Iterable<T> означает, что можно производить итерацию по классу T, который указан в параметрах
интерфейс Iterator<T> - инструмент, с помощью которого производится итерация

Самый базовый интерфейс всех коллекций Collection наследуется от Iterable<T> - поэтому любую коллекцию можно итерировать
(в том числе с помощью for each loop)

1. Отнаследовать интерфейс TaskBoard от Iterable<Task>
2. Реализовать метод public Iterator<Task> iterator() (и только его). Дам подсказку (не хочешь, дальше лучше не читай): надо просто вызвать итератор у тасок
3. Попробуй в мейне создать таскбоард и распечатать каждую таску следующим образом
        for (Task task : taskBoard) {
            System.out.println(task);
        }

4. Попробуй метод removeTask в DefaultTaskBoard переписать через итератор и while loop (методы hasNext и next)
